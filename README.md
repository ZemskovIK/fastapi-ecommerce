# FastAPI E-commerce

API интернет-магазина на FastAPI с аутентификацией, ролями (buyer/seller/admin), товарами, категориями и отзывами.

## 🚀 Функциональность

- **Пользователи**: регистрация, вход, JWT-аутентификация (access + refresh токены)
- **Роли**: 
  - `buyer` — может просматривать товары и оставлять отзывы
  - `seller` — может управлять своими товарами
  - `admin` — управляет категориями и ролями пользователей
- **Категории**: вложенные (parent-child), CRUD (только для админов)
- **Товары**: 
  - просмотр — публичный,
  - создание/редактирование/удаление — только для продавцов (только свои товары)
- **Отзывы**: 
  - оставить отзыв может только покупатель,
  - автоматический пересчёт рейтинга товара,
  - мягкое удаление

## 🛠 Технологии

- **Backend**: FastAPI
- **База данных**: PostgreSQL (асинхронно через asyncpg)
- **ORM**: SQLAlchemy 2.0 (асинхронный режим)
- **Миграции**: Alembic
- **Аутентификация**: JWT (PyJWT), bcrypt (passlib)
- **Валидация**: Pydantic v2

## 📦 Установка и запуск

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ZemskovIK/fastapi-ecommerce.git
cd fastapi-ecommerce
```

2. Создайте виртуальное окружение и установите зависимости:
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или .venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

3. Настройте переменные окружения:

Создайте файл `.env` на основе `.env.example`:
```env
DATABASE_URL="postgresql+asyncpg://username:password@host:port/database"
SECRET_KEY=ваш_секретный_ключ
```

4. Примените миграции:
```bash
alembic upgrade head
```

5. Запустите сервер:
```bash
uvicorn app.main:app --reload
```

6. Откройте http://localhost:8000/docs — интерактивная документация Swagger UI.

## 🔐 Админка
Для работы с категориями и ролями нужен пользователь с ролью `admin`.

Создайте его вручную в БД или через эндпоинт (если реализован):
```sql
INSERT INTO users (email, hashed_password, role, is_active)
VALUES ('admin@example.com', '<хеш_пароля>', 'admin', true);
```
>Хеш пароля можно получить через `hash_password()` из `app/auth.py`.

## 📂 Структура проекта

```
app/
├── auth.py          # JWT, хеширование, зависимости аутентификации
├── config.py        # Загрузка .env
├── database.py      # AsyncEngine, сессии
├── db_depends.py    # get_async_db
├── main.py          # Точка входа
├── models/          # SQLAlchemy модели
├── routers/         # Эндпоинты
├── schemas.py       # Pydantic схемы
└── migrations/      # Alembic
```